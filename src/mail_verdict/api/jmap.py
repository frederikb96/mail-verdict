"""
JMAP delta sync endpoint.

FUTURE: JMAP push/delta sync for efficient real-time mail updates.
Not implemented in v1 — SSE via IMAP IDLE events is used instead.
"""

# FUTURE: Implement JMAP delta sync when needed
# - JMAP Email/changes for efficient delta updates
# - JMAP EventSource for push notifications
# - Would replace polling-based IMAP sync for supported servers

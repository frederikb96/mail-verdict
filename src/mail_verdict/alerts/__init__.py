"""
Alerts: a durable record of something worth interrupting the reader for --
new mail today, a calendar reminder in a later feature -- delivered over
the in-app path (an open page reacting to the SSE stream). See
dispatch.py for how a mail arrival becomes one, and api/alerts.py for the
read/dismiss surface.
"""
